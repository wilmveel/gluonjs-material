source: '@material/button/mdc-button'
mappings:

  keyframes:
    type: keyframes

  host:
    type: rule
    regex: ^(\.mdc-button.*?)((?:\:\:).*)?$
    replace: :host($1)$2

  slotted:
    type: rule
    regex: ^(\.mdc-button.*?)((?:\:\:).*)?$
    replace: ::slotted($1)$2

