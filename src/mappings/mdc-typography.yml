source: '@material/typography/mdc-typography'
mappings:

  host:
    type: rule
    regex: ^(\.mdc-typography--.*)$
    replace: :host($1)

  slotted:
    type: rule
    regex: ^(\.mdc-typography--.*)$
    replace: ::slotted($1)

