source: '@material/icon-toggle/mdc-icon-toggle'
mappings:

  keyframes:
    type: keyframes

  all:
    type: rule
    regex: ^(.*)$
    replace: $1

  icon:
    type: rule
    regex: ^(\.mdc-icon-toggle.*?)((?:\:\:).*)?$
    replace: :host($1)$2
